server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/takeout?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

wechat:
  app:
    id: wx1234567890abcdef
    secret: abcdef1234567890abcdef1234567890

jwt:
  secret: your-secret-key
  expiration: 86400000

logging:
  level:
    root: info
    api.cssc.ciallo.games: debug
    org.springframework.web: debug
    org.hibernate: debug

upload:
  path: ../uploads